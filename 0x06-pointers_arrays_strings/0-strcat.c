#include "main.h"

/**
 * _strcat - append src to dest
 * @dest: dest
 * @src: src
 *
 * Return: address dest
 */

char *_strcat(char *dest, char *src)
{
	int i, j;

	i = j = 0;
	while (*(dest + 1))
		i++;

	while ((*(dst + i) = *(src + j)))
	{
		i++;
		j++
	};
	return (dest);
}
