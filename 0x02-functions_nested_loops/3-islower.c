#include "main.h"

/**
 * _abs -  determine teh absolute value of an integer
 * @n: The number to be be checked
 *
 * Return: absolute value
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
